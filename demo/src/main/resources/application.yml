spring:
  application:
    name: crop-yield-platform
  profiles:
    active: h2

server:
  port: 8080

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG

security:
  jwt:
    secret: "cropyield-platform-secret-key-change-me"
    expiration: 86400000 # 1 day in milliseconds
